import React from 'react';
import styled, { keyframes } from 'styled-components';

const AboutBaseContainter = styled.div`
  display: flex;
`;

const AboutSpotivibe = () => {

  return (<AboutBaseContainter>

  </AboutBaseContainter>);
};

export default AboutSpotivibe;