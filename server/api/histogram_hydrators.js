// takes hitograms and hydrates the objects in them
// each histogram should look like { <thing>_id, occurances }
async function histogramHydrator(
  user,
  spotifyClient,
  track_histogram,
  artist_histogram,
  album_histogram
) {
  
}
